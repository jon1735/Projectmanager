<h1> List of All Manageable Properties </h1>

<ul>
  <% @properties.each do |property|%>
    <li>
      <%= link_to property.address, property_path(property)%>
    </li>
  <% end %>
</ul>

<button><%= link_to "Create a new property", new_property_path %> </button>